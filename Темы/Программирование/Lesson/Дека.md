**Дека** - это абстрактный объект, поддерживающий четыре операции.  Добавление в начало (`pushfront`), добавление в конец(`pushback`), извлечение из начала(`popfron`) и извлечение из конца(`popback`).